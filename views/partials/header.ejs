<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <header class="topbar">
    <div class="container topbar__inner">
      <div class="brand">
        <span class="brand__logo">⚡</span>
        <span class="brand__name">Mon Site Express</span>
      </div>

      <div class="welcome">
        <% if (login) { %>
          <span>Bienvenue <strong><%= login %></strong></span>
        <% } else { %>
          <span>Mode visiteur</span>
        <% } %>
      </div>
    </div>
  </header>
