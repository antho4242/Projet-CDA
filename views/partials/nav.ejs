<nav class="nav">
  <div class="container nav__inner">
    <a class="nav__brand" href="/">Maroquinerie</a>

    <a class="nav__link" href="/"><%- icon('home', 16) %> Accueil</a>
    <a class="nav__link" href="/boutique"><%- icon('store', 16) %> Boutique</a>
    <a class="nav__link" href="/dab"><%- icon('dab', 16) %> DAB</a>
    <a class="nav__link nav__link--danger" href="/erreur"><%- icon('error', 16) %> Erreur</a>

    <% if (user && user.role === 'gestionnaire') { %>
      <a class="nav__link" href="/dashboard"><%- icon('dashboard', 16) %> Dashboard</a>
    <% } %>

    <% if (user && user.role === 'client') { %>
      <a class="nav__link" href="/panier"><%- icon('cart', 16) %> Panier</a>
      <a class="nav__link" href="/espace-client"><%- icon('user', 16) %> Mon espace</a>
    <% } %>

    <div class="nav__spacer"></div>

    <% if (!user) { %>
      <a class="btn btn--ghost" href="/auth/login"><%- icon('login', 15) %> Connexion</a>
      <a class="btn btn--accent" href="/auth/register"><%- icon('register', 15) %> S'inscrire</a>
    <% } else { %>
      <span class="nav__welcome">
        <%- icon('user', 14) %>
        <%= user.prenom %>
        <span class="role-badge role-badge--<%= user.role %>"><%= user.role %></span>
      </span>
      <form action="/auth/logout" method="POST">
        <button class="btn btn--ghost" type="submit">
          <%- icon('logout', 15) %> Déconnexion
        </button>
      </form>
    <% } %>
  </div>
</nav>