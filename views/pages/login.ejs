<%- include("../partials/header", { title: "Connexion" }) %>
<%- include("../partials/nav") %>

<main class="container main">
  <section class="card card--narrow">
    <h1>🔐 Connexion</h1>
    <p style="color:var(--muted)">Connectez-vous en tant que client ou gestionnaire.</p>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="alert alert--error"><%= error %></div>
    <% } %>

    <form action="/auth/login" method="POST" class="form">
      <div class="form__group">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" placeholder="votre@email.com" required />
      </div>
      <div class="form__group">
        <label for="password">Mot de passe</label>
        <input type="password" id="password" name="password" placeholder="••••••" required />
      </div>
      <button type="submit" class="btn btn--primary">Se connecter</button>
    </form>

    <p style="margin-top:16px; color:var(--muted); font-size:14px;">
      Pas encore de compte ?
      <a href="/auth/register" style="color:var(--accent)">S'inscrire</a>
    </p>
  </section>
</main>

<%- include("../partials/footer") %>